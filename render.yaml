services:
  - type: web
    name: drishtiguard-live-demo
    runtime: docker
    plan: free
    dockerfilePath: ./apps/live-demo/Dockerfile
    dockerContext: ./apps/live-demo
    healthCheckPath: /api/health
    envVars:
      - key: NEXT_TELEMETRY_DISABLED
        value: "1"
